@model MezoExperts.Models.LoginModel

@using (Html.BeginForm("LoginExpert", "Account",FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary()

    <div class="modal fade" id="loginExpertModal" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-content">
            <div class="close-modal" data-dismiss="modal">
                <div class="lr">
                    <div class="rl">
                    </div>
                </div>
            </div>
            <div class="container text-center">
                <div class="row">
                    <div class="col-lg-8 col-lg-offset-2">
                        <div class="modal-body">
                            <h2>Expert Login</h2>
                            <hr class="star-primary">
                            <p>Fill the following details to login to your account.</p>
                            @*<form role="form">*@

                            <div class="form-group">
                                <div class="input-group">
                                    @Html.TextBoxFor(m => m.UserName, new { placeholder = "Email Address", @class = "form-control", type = "email" })
                                    @*<input type="text" class="form-control" id="uLogin" placeholder="Login">*@
                                    <label for="uLogin" class="input-group-addon glyphicon glyphicon-user"></label>

                                </div>
                                @Html.ValidationMessageFor(m => m.UserName)
                            </div> <!-- /.form-group -->
                            <div class="form-group">
                                <div class="input-group">
                                    @Html.PasswordFor(m => m.Password, new { placeholder = "Password", @class = "form-control" })
                                    @*<input type="password" class="form-control" id="uPassword" placeholder="Password">*@
                                    <label for="uPassword" class="input-group-addon glyphicon glyphicon-lock"></label>
                                </div> <!-- /.input-group -->
                                @Html.ValidationMessageFor(m => m.Password)
                            </div> <!-- /.form-group -->
                            <div class="checkbox">
                                <label>
                                    @*<input type="checkbox">*@
                                    @Html.CheckBoxFor(m => m.RememberMe) Remember me
                                </label>
                            </div> <!-- /.checkbox -->

                            <input type="submit" class="btn btn-success" value="Login" />

                            <button type="button" class="btn btn-default" data-dismiss="modal"><i class="fa fa-times"></i> Close</button>
                        </div>
                        <p class="error">@TempData["ExpertLoginError"]</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

        